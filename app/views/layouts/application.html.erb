<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, intial-scale=1.0">
	<meta name="Happening Now" content="">
	<meta name="Joe Jack Chris Tom Dan" content="">
	<link rel="shortcut icon" href="assets/img/favicon.png">

  <title>Happening Now</title>

  <%= stylesheet_link_tag "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <link href="http://fonts.googleapis.com/css?family=Lato:300,400,900" rel="stylesheet" type="text/css">
</head>

<body>
<%= render 'layouts/navbar' %>
  <% session[:next_urls] ||= [] %>
	<!-- we can put this in a partial if we decide on doing a nav bar -->
	<% flash.each do |name, msg| %>
		<% if msg.is_a?(String) %>
			<div class="alert alert-<%= name == :notice ? "success" : "danger" %>">
				<button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
				<%= content_tag :div, msg, :id => "flash_#{name}" %>
			</div>
		<%end%>
	<%end%>
	  <%= yield %>
</body>
<footer>
	  <div class="container">
		<hr>
				<p class="centered"><i class="glyphicon glyphicon-globe"></i> Created by Happening Now. Dev Bootcamp Foxes 2014.</p>
	  </div>

</footer>
</html>
